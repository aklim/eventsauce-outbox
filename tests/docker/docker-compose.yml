version: '3'

services:
    php:
        build:
            context: ./php8
        container_name: eventsauce_outbox_php
        volumes:
            - ../../:/var/library:rw
        networks:
            - default
networks:
    default:
